import { ValueHidePipe } from './value-hide.pipe';

describe('ValueHidePipe', () => {
  it('create an instance', () => {
    const pipe = new ValueHidePipe();
    expect(pipe).toBeTruthy();
  });
});
